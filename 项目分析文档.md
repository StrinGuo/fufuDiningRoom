# 福福餐厅 (fufu_dining_room) 项目分析文档

## 📋 项目概述

**项目名称**: fufu_dining_room (福福餐厅)  
**项目类型**: Flutter 跨平台移动应用  
**当前版本**: 1.0.0+1  
**开发状态**: 初始阶段（仅完成基础框架搭建）

---

## 🛠️ 技术栈

### 核心框架
- **Flutter SDK**: ^3.9.2
- **Dart**: 3.9.2+
- **后端服务**: Supabase (supabase_flutter: ^2.1.2)

### 支持平台
- ✅ Android
- ✅ iOS
- ✅ Web
- ✅ Windows
- ✅ Linux
- ✅ macOS

### 开发工具
- **代码规范**: flutter_lints ^5.0.0
- **构建工具**: Gradle (Android), Xcode (iOS/macOS), CMake (Windows/Linux)

---

## 📁 项目结构分析

```
fufu_dining_room/
│
├── lib/                          # 📱 客户端代码主目录（核心开发区域）
│   └── main.dart                 # 应用入口文件
│
├── android/                      # Android 平台配置
│   ├── app/
│   │   └── build.gradle.kts      # Android 构建配置
│   └── build.gradle.kts          # 项目级 Gradle 配置
│
├── ios/                          # iOS 平台配置
│   └── Runner/                   # iOS 应用配置
│
├── web/                          # Web 平台配置
│   ├── index.html               # Web 入口
│   └── manifest.json            # Web 清单文件
│
├── windows/                      # Windows 平台配置
├── linux/                        # Linux 平台配置
├── macos/                        # macOS 平台配置
│
├── test/                         # 测试代码目录
│   └── widget_test.dart
│
├── pubspec.yaml                  # 项目依赖和配置
├── analysis_options.yaml         # 代码分析配置
└── README.md                     # 项目说明
```

---

## 🔍 当前代码状态

### 主要功能
目前项目处于**初始阶段**，仅完成了以下基础功能：

1. **Supabase 初始化**
   - 已配置 Supabase 连接
   - URL: `https://mnzjejtrgcnoulwxpets.supabase.co`
   - 已初始化 Supabase 客户端

2. **基础 UI**
   - 简单的 Material Design 界面
   - 仅显示 "Supabase 连接成功！" 文本

### 代码位置
- **主入口**: `lib/main.dart`
- **当前架构**: 单文件架构（所有代码在一个文件中）

---

## 🏗️ 客户端开发指南

### 📍 在哪里写客户端代码？

**核心开发目录**: `lib/` 文件夹

所有客户端业务逻辑、UI界面、状态管理、数据模型等都应该在 `lib/` 目录下组织。

### 🎯 推荐的目录架构

建议采用以下分层架构结构：

```
lib/
├── main.dart                    # 应用入口（初始化、路由配置）
│
├── core/                        # 核心功能层
│   ├── config/                  # 配置文件
│   │   └── supabase_config.dart # Supabase 配置（从 main.dart 移出）
│   ├── constants/               # 常量定义
│   ├── theme/                    # 主题配置
│   └── utils/                    # 工具类
│
├── data/                        # 数据层
│   ├── models/                  # 数据模型
│   │   ├── user_model.dart
│   │   ├── menu_item_model.dart
│   │   └── order_model.dart
│   ├── repositories/            # 数据仓库
│   │   ├── user_repository.dart
│   │   └── menu_repository.dart
│   └── services/                # 数据服务
│       └── supabase_service.dart
│
├── domain/                      # 业务逻辑层（可选，用于大型项目）
│   └── usecases/                # 业务用例
│
├── presentation/                # 表现层（UI）
│   ├── screens/                 # 页面/屏幕
│   │   ├── home/
│   │   │   ├── home_screen.dart
│   │   │   └── home_viewmodel.dart
│   │   ├── menu/
│   │   │   └── menu_screen.dart
│   │   └── order/
│   │       └── order_screen.dart
│   │
│   ├── widgets/                 # 可复用组件
│   │   ├── menu_item_card.dart
│   │   └── order_summary.dart
│   │
│   └── providers/               # 状态管理（如果使用 Provider/Riverpod）
│       └── menu_provider.dart
│
└── routes/                      # 路由配置
    └── app_router.dart
```

---

## 🏛️ 架构模式推荐

### 方案一：MVVM 架构（推荐）

**适合场景**: 中小型项目，需要清晰的状态管理

```
Model (数据模型)
  ↓
ViewModel (业务逻辑 + 状态管理)
  ↓
View (UI 界面)
```

**技术栈建议**:
- **状态管理**: Provider / Riverpod / GetX
- **数据获取**: Supabase Client 直接调用
- **路由**: go_router / auto_route

**示例结构**:
```
lib/
├── models/          # Model
├── viewmodels/      # ViewModel
└── screens/         # View
```

### 方案二：Clean Architecture（大型项目）

**适合场景**: 大型项目，需要高度解耦和可测试性

```
Presentation Layer (UI)
  ↓
Domain Layer (业务逻辑)
  ↓
Data Layer (数据源)
```

**技术栈建议**:
- **状态管理**: Riverpod / Bloc
- **依赖注入**: get_it / riverpod
- **数据层**: Repository Pattern

### 方案三：简单分层架构（当前阶段推荐）

**适合场景**: 快速开发，项目初期

```
lib/
├── models/          # 数据模型
├── services/        # 数据服务（Supabase 调用）
├── screens/         # 页面
└── widgets/         # 组件
```

---

## 📝 开发建议

### 1. 立即需要做的事情

#### ✅ 重构当前代码
- [ ] 将 Supabase 配置从 `main.dart` 移到 `lib/core/config/supabase_config.dart`
- [ ] 创建基础路由配置
- [ ] 设置应用主题

#### ✅ 创建基础架构
- [ ] 创建 `lib/models/` 目录（数据模型）
- [ ] 创建 `lib/screens/` 目录（页面）
- [ ] 创建 `lib/widgets/` 目录（组件）
- [ ] 创建 `lib/services/` 目录（服务层）

#### ✅ 添加状态管理
推荐使用以下之一：
- **Provider** (简单易用)
- **Riverpod** (类型安全，推荐)
- **GetX** (功能全面)

### 2. 下一步开发计划

1. **用户认证模块**
   - 登录/注册页面
   - 用户状态管理
   - 会话管理

2. **菜单展示模块**
   - 菜单列表页面
   - 菜单详情页面
   - 分类筛选

3. **订单管理模块**
   - 购物车
   - 订单提交
   - 订单历史

4. **个人中心模块**
   - 用户信息
   - 订单记录
   - 设置

### 3. 代码规范

- 遵循 `analysis_options.yaml` 中的 lint 规则
- 使用有意义的文件名和类名
- 保持代码模块化和可复用性
- 添加必要的注释（特别是业务逻辑）

---

## 🔧 开发环境配置

### 必需工具
- Flutter SDK 3.9.2+
- Dart SDK 3.9.2+
- Android Studio / VS Code
- Android SDK (Android 开发)
- Xcode (iOS 开发，仅 macOS)

### 运行项目
```bash
# 安装依赖
flutter pub get

# 运行应用（选择设备）
flutter run

# 构建 Android APK
flutter build apk

# 构建 iOS
flutter build ios
```

---

## 📦 当前依赖分析

### 生产依赖
- `flutter`: SDK
- `supabase_flutter: ^2.1.2`: Supabase 客户端
- `cupertino_icons: ^1.0.8`: iOS 风格图标

### 开发依赖
- `flutter_test`: 测试框架
- `flutter_lints: ^5.0.0`: 代码规范检查

### 建议添加的依赖

**状态管理**:
```yaml
provider: ^6.1.1
# 或
flutter_riverpod: ^2.5.1
# 或
get: ^4.6.6
```

**路由**:
```yaml
go_router: ^13.0.0
```

**UI 组件**:
```yaml
cached_network_image: ^3.3.1  # 图片缓存
shimmer: ^3.0.0                # 加载动画
```

**工具类**:
```yaml
shared_preferences: ^2.2.2     # 本地存储
dio: ^5.4.0                    # HTTP 请求（如果需要）
```

---

## 🎨 UI/UX 建议

### 设计系统
- 使用 Material Design 3 (Flutter 默认支持)
- 定义统一的颜色主题（在 `lib/core/theme/` 中）
- 创建可复用的组件库

### 响应式设计
- 考虑不同屏幕尺寸
- 使用 `LayoutBuilder` 或 `MediaQuery` 适配
- 测试不同设备（手机、平板、桌面）

---

## 🔐 安全注意事项

### ⚠️ 重要提醒

当前代码中 **Supabase 密钥直接硬编码在代码中**，这是不安全的做法！

**建议**:
1. 使用环境变量或配置文件（不提交到 Git）
2. 使用 `flutter_dotenv` 包管理环境变量
3. 在 `.gitignore` 中排除配置文件

---

## 📊 项目状态总结

| 项目阶段 | 状态 | 完成度 |
|---------|------|--------|
| 项目初始化 | ✅ 完成 | 100% |
| Supabase 集成 | ✅ 完成 | 100% |
| 基础架构 | ❌ 未开始 | 0% |
| UI 界面 | ⚠️ 仅示例 | 5% |
| 业务逻辑 | ❌ 未开始 | 0% |
| 状态管理 | ❌ 未开始 | 0% |
| 路由配置 | ❌ 未开始 | 0% |

---

## 🚀 快速开始开发

### 第一步：创建基础目录结构
```bash
mkdir -p lib/core/config
mkdir -p lib/models
mkdir -p lib/screens
mkdir -p lib/widgets
mkdir -p lib/services
mkdir -p lib/routes
```

### 第二步：重构 main.dart
将 Supabase 配置提取到独立文件，设置路由和主题。

### 第三步：选择状态管理方案
根据项目复杂度选择合适的方案（推荐 Riverpod）。

### 第四步：开始开发第一个功能模块
建议从用户认证或菜单展示开始。

---

## 📚 参考资源

- [Flutter 官方文档](https://docs.flutter.dev/)
- [Supabase Flutter 文档](https://supabase.com/docs/reference/dart/introduction)
- [Flutter 架构指南](https://docs.flutter.dev/development/data-and-backend/state-mgmt/options)
- [Material Design 3](https://m3.material.io/)

---

**文档生成时间**: 2024年  
**项目路径**: `C:\Users\Administrator\StudioProjects\fufu_dining_room`

