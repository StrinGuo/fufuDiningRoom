# 最小化修复配置
# 只添加这一部分到你的 Nginx 配置中

# 在 server 块内添加或修改 location / 块：
location / {
    try_files $uri $uri/ /index.html;
}

# 注意：
# 1. 这必须放在 server { } 块内
# 2. 如果已有 location / 块，替换 try_files 这一行即可
# 3. 不要删除其他 location 块（如 location ~ \.well-known）

